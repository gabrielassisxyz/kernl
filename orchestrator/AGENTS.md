# Kernl Orchestrator — Agent Briefing

This subdirectory inherits the master agent briefing at the repository root.

**Read:** [`../AGENTS.md`](../AGENTS.md)

Anything specific to the orchestrator block (paths, package conventions) is
either captured in `specs/` (authoritative per-subsystem behavior) or in the
root `AGENTS.md` (cross-cutting rules).

Do not maintain divergent stack/style rules here — the root file wins.
