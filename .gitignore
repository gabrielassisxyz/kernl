/kernl
/migrate.go
# Stray bookmark archives written by tests when Vault.Root is unset (resolves
# relative to the package cwd). Real archives live under <Vault.Root>/.kernl.
internal/api/.kernl/
orchestrator/kernl
orchestrator/.gitignore
orchestrator/AGENTS.md
orchestrator/opencode-config.json
orchestrator/.claude/
orchestrator/specs/
*.exe
*.dll
*.so
*.dylib
*.test
*.out
go.work
go.work.sum
/vendor/
/pkg/

.vscode/
.idea/
*.swp
*.swo
*~

.DS_Store
Thumbs.db
._*

__pycache__/
*.pyc
*.pyo
.venv/
.env
.envrc
.pytest_cache/

.dolt/
*.db
*.db-journal
*.db-wal
*.db-shm
.beads-credential-key

# No trailing slash on the paths listed in `.worktree-links`: bin/worktree materializes
# them as SYMLINKS in every worktree, and a `dir/` pattern matches only a real directory.
# With the slash the link is untracked, so `git add -A` in a worktree stages it.
.claude
.opencode
.gemini
.codex
.cursor/
.augment/
.agents
.github/
.impeccable/
.vibe-chaos-to-concept/
CLAUDE.md
CLAUDE.local.md
AGENTS.md

# Solutions are compound-engineering artifacts; they live in the wiki vault, not in git.
docs/solutions/

.bv
.beads

kernl.yaml
!kernl.yaml.example
*.env
*.local
*.local.*
*.log

RESEARCH_FINDINGS.md
TOON_INTEGRATION_BRIEF.md
SESSION_REPORT.md
SESSION_TODO.md
SESSION_*.md
artifact-*.json
artifacts
*.mcp.json
*.mcp.json.*.bak
opencode.json
.agent-mail-project-id
.agent-mail.yaml

/.gocache/
/.gomodcache/

coverage.out
coverage.html
*.prof
*.cover

node_modules/

# Frontend build artifacts (Nuxt/Vue)
web/.nuxt/
web/.output/
web/dist

*.bak
*.backup
*.tmp
.tmp*/
.scratch/

result
result-*

scripts/
update_binary.sh
generate-env.sh
run.sh

# Beads compliance audit dir (managed by /beads-compliance-and-completion-verification skill)
/beads_compliance_audit/

.ai-jail
.gstack/
.stitch/

# goreleaser build output
/dist/

# >>> repo-harness:gitignore-private-boundary >>>
# Maintainer notes — not part of the project; they live in the private notes repo,
# reachable through the `local` symlink. NO trailing slash on any path in this block:
# a worktree materializes these paths as SYMLINKS, and a `foo/` pattern matches a real
# directory only — the ignore then keeps working in the main tree and silently fails
# in every worktree.
/local

# Environment secrets and the per-machine agent sandbox config.
.env
.ai-jail
# <<< repo-harness:gitignore-private-boundary <<<
